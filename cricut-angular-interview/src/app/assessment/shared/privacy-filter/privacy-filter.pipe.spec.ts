import { PrivacyFilterPipe } from './privacy-filter.pipe';

describe('PrivacyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrivacyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
